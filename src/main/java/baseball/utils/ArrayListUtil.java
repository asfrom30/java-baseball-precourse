package baseball.utils;

public class ArrayListUtil {

    // TODO: T형 클래스 (현재는 String[])으로 변환
    // Score.getValues
    public void toArray() {
//        return this.values.toArray(new String[this.values.size()]);
    }
}
